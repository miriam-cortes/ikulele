//= require_tree ./components

$(document).on('click', '#favorite', function(e) {
  $.ajax({
    url: "/sessions/favorites",
    type: "POST",
    data: {"song_id": $('.hidden').text() },
    success: function(data){
      alert("success!");
    }
  });
});
