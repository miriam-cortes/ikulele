<h3 class="cursive-font"><%= @artist.name %></h3>


<h4 class="second-font">
  <% @artist.songs.each do |song| %>
    <%= link_to song.name, artist_song_path(@artist.id, song.id) %>
  <% end %>
</h4>
