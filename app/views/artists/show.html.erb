<h3 class="cursive-font small-12 columns"><%= @artist.name %></h3>


<div class="small-12 columns">
  <% @artist.songs.each do |song| %>
    <h4 class="skinny-font"><%= link_to song.name, artist_song_path(@artist.id, song.id) %></h4>
  <% end %>
</div>
